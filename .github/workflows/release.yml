name: Release Windows installer

on:
  push:
    tags: ['v[0-9]*']
  workflow_dispatch:
    inputs:
      tag:
        description: 'Version to validate (must match pyproject.toml); manual runs upload artifacts only'
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: release-${{ github.repository }}-${{ inputs.tag || github.ref_name }}
  cancel-in-progress: false

jobs:
  package:
    runs-on: windows-2022
    timeout-minutes: 60
    env:
      RELEASE_TAG: ${{ inputs.tag || github.ref_name }}
      RELEASE_VISIBILITY: ${{ github.event.repository.visibility }}
      PYTHONUTF8: '1'
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}
          persist-credentials: false
      - uses: astral-sh/setup-uv@v6
        with:
          version: '0.12.15'
      - uses: actions/setup-node@v4
        with:
          node-version: '22.23.2'
      - name: Install test dependencies
        shell: pwsh
        run: |
          uv sync --frozen --python 3.12 --extra server
          if ($LASTEXITCODE) { throw 'Python dependency installation failed.' }
          .venv/Scripts/python.exe -c "from hindsightkit.cli import install_node_packages; install_node_packages(); install_node_packages(client=True)"
          if ($LASTEXITCODE) { throw 'Node dependency installation failed.' }
      - name: Run tests
        shell: pwsh
        run: |
          .venv/Scripts/python.exe -m unittest discover -s tests -v
          if ($LASTEXITCODE) { throw 'Tests failed.' }
      - name: Build the pinned PostgreSQL distribution
        shell: pwsh
        run: |
          $distribution = Join-Path $env:RUNNER_TEMP 'hindsightkit-postgres/pgsql'
          $cache = Join-Path $env:RUNNER_TEMP 'hindsightkit-postgres-downloads'
          ./src/hindsightkit/postgres_install.ps1 -Destination $distribution -CacheDirectory $cache
          if ($LASTEXITCODE) { throw 'PostgreSQL packaging failed.' }
          .venv/Scripts/python.exe tests/live_postgres.py --distribution $distribution
          if ($LASTEXITCODE) { throw 'PostgreSQL runtime validation failed.' }
      - name: Build and verify offline Python packages
        shell: pwsh
        run: |
          .venv/Scripts/python.exe distribution/build_python_bundle.py --source-root . --output dist/python
          if ($LASTEXITCODE) { throw 'Python package bundle failed.' }
          .venv/Scripts/python.exe distribution/verify_python_install.py --bundle dist/python --uv uv
          if ($LASTEXITCODE) { throw 'Offline Python installation validation failed.' }
      - name: Build and verify offline Node components
        shell: pwsh
        run: |
          .venv/Scripts/python.exe distribution/build_node_bundle.py --source-root . --output dist/node
          if ($LASTEXITCODE) { throw 'Node component bundle or offline validation failed.' }
      - name: Package validated release assets
        shell: pwsh
        run: |
          $distribution = Join-Path $env:RUNNER_TEMP 'hindsightkit-postgres/pgsql'
          .venv/Scripts/python.exe distribution/package_release.py --version $env:RELEASE_TAG --repository $env:GITHUB_REPOSITORY --server-url $env:GITHUB_SERVER_URL --visibility $env:RELEASE_VISIBILITY --postgres-directory $distribution --python-directory dist/python --node-directory dist/node --output dist/release
          if ($LASTEXITCODE) { throw 'Release packaging failed.' }
      - name: Verify Python installation from the final application archive
        shell: pwsh
        run: |
          Expand-Archive -LiteralPath dist/release/hindsightkit-windows-x64.zip -DestinationPath dist/verified-application
          .venv/Scripts/python.exe distribution/verify_python_install.py --bundle dist/verified-application/app/python --uv uv
          if ($LASTEXITCODE) { throw 'Packaged offline installation failed.' }
          Expand-Archive -LiteralPath dist/release/hindsightkit-client-windows-x64.zip -DestinationPath dist/verified-client
          .venv/Scripts/python.exe distribution/verify_python_install.py --bundle dist/verified-client/app/python --uv uv
          if ($LASTEXITCODE) { throw 'Client package offline installation failed.' }
          .venv/Scripts/python.exe distribution/verify_node_install.py --bundle dist/verified-application/app/node --package dist/verified-application/app/src/hindsightkit
          if ($LASTEXITCODE) { throw 'Packaged server Node installation failed.' }
          .venv/Scripts/python.exe distribution/verify_node_install.py --bundle dist/verified-client/app/node --package dist/verified-client/app/src/hindsightkit
          if ($LASTEXITCODE) { throw 'Packaged client Node installation failed.' }
      - uses: actions/upload-artifact@v4
        with:
          name: windows-release
          path: dist/release/*
          if-no-files-found: error

  publish:
    if: github.event_name == 'push'
    needs: package
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      RELEASE_TAG: ${{ inputs.tag || github.ref_name }}
      GH_TOKEN: ${{ github.token }}
      GH_REPO: ${{ github.repository }}
      GH_HOST: github.com
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: windows-release
          path: release
      - name: Create draft release
        shell: bash
        run: |
          set -euo pipefail
          export GH_HOST="${GITHUB_SERVER_URL#https://}"
          gh release create "$RELEASE_TAG" --verify-tag --draft --prerelease --title "HindsightKit $RELEASE_TAG" --notes-file release/release-notes.md release/install.ps1 release/hindsightkit-windows-x64.zip release/hindsightkit-client-windows-x64.zip release/postgresql-18.6-pgvector-0.8.6-windows-x64.zip release/QUICKSTART.md release/release-notes.md release/SHA256SUMS
