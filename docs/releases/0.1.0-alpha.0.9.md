# ProvenLoop 0.1.0-alpha.0.9

**Date:** 2026-09-06
**Status:** Failed publication — no GitHub Release or assets
**Superseded by:** [0.1.0-alpha.0.10](0.1.0-alpha.0.10.md)

The `v0.1.0-alpha.0.9` tag at `c5a48fc` remains immutable.
[Release run 34002088945](https://github.com/cubika/ProvenLoop/actions/runs/34002088945)
failed the `production-learning-loop` real-runtime source assertion: context
status was `degraded` rather than `ok`, with 112 ms reported on a loaded runner.
This does not establish the exact failure cause. Local runs passed 472 unit and
244 integration tests; any separate main-CI outcome does not change the failed
release or create assets. This master-copy correction does not change the tag.

This was an attempted successor to the [failed 0.8 publication](0.1.0-alpha.0.8.md),
not an installable Release tarball.
**M0/MVP remain No-Go**, controlled field benefit is unproved, and
`0.1.0-alpha.1` remains an unapproved quality-release target.

## Superseding candidate

Do not install 0.9 from GitHub Release assets: none were created. Use the
[0.10 candidate notes](0.1.0-alpha.0.10.md) and
[installation and operations](../alpha-installation.md) for the next versioned
installer, prerequisites, opt-outs, upgrade, and rollback.

## Attempted worker validation fixes

- Package smoke distinguishes explicit resource-pressure/circuit pauses from
  errors and retains both stdout and stderr diagnostics. It also requires the
  installed runtime worker to complete under controlled test admission; a
  legitimate pause alone does not satisfy that completion check.
- The current-session reconciliation fixture uses the existing injected
  admission control rather than depending on ambient runner load. Product
  resource thresholds, runtime deadlines, and safety checks remain unchanged.

The 0.8 smoke treated a potentially legitimate resource pause as a failure;
its surfaced diagnostics did not establish the exact worker reason. Release
lint, typecheck, unit, and integration checks passed for 0.8, but package smoke
failed. The 0.9 release failed a different functional context assertion; neither
failure establishes the exact cause of the other.

## Attempted learning and runtime scope

This candidate carried forward the full attempted 0.8 scope:

- Native SDK causal verification bridge and `assistant.turn_start` mapping.
  Correction proof is bound to the operation, repository, and worktree;
  old confirmations cannot erase newer counterevidence.
- Trusted live SDK Session registry with a cross-process reader-probe guard
  and explicit real-user approval for persistent MCP feedback.
- Natural English/Chinese retrieval and corrected exclusion matching.
  Task-start instructions request relevant context without guaranteeing host use.
- Knowledge list/show/confirm/replace/revoke with reviewed digests and explicit
  confirmation. Workflow controls require a matching live `SESSION_ID`,
  workflow, and cwd; flags alone do not authorize access.
- Automatic local observation summaries and privacy-minimized exports separate
  context exposure, explicitly reported adoption, and unknown outcomes.
- Bounded current-session reconciliation in the installed Extension background
  loop strictly matches SDK `sessionId`/`workspacePath`, validates state/path/lease
  boundaries, and excludes pre-join records. Worker/admission runs first, then
  reconciliation and observations every 30 seconds; actual progress or budget
  exhaustion schedules two-second catch-up. Shutdown stops the loop.
- Two-pass capture-metadata redaction, append-only enrichment, corrupt-queue
  isolation, acknowledged-item pruning, transactional deletion, restore
  tombstone checks, and schema-10 maintenance with pre-upgrade snapshots.

## Upgrade and known limitations

- For upgrades from the previous published 0.7 candidate, follow the 0.10 notes.
  Keep recovery snapshots and the prior runtime slot. Integration rollback does
  not make an older runtime compatible with schema 10; there is no public
  restore CLI. Preserve deletion tombstones during maintainer-assisted recovery.
- Missing or mismatched SDK workspace metadata produces a diagnostic and skips
  automatic backfill. Capture is best effort, not lossless archival or full
  historical ingestion; reconciliation is bounded to the current observation
  window and supported accessible records.
- Pending/dead-letter queue data has no total storage quota. Acknowledged-item
  pruning does not limit backlog when the worker is paused.
- Windows/platform, paired latency, provider-degradation, native-host recovery,
  and two-version remote-upgrade acceptance still require retained evidence.
- Synthetic fixtures, local observations, and maintainer attestations cannot
  clear `field-effect-evidence`. Exposure is not adoption, helpful feedback is
  not verified success, and unknown safety/outcome counts are not zero.
- Local validation did not make the 0.9 release successful. No retained
  native-host acceptance or controlled-benefit result is claimed here.
